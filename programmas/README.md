# Programmas media

Place portfolio media in this folder:

- `project-demo.mp4` - local project video
- `project-photo.jpg` - project screenshot or photo
- `video-cover.jpg` - optional poster image for the video

The YouTube preview is configured in `index.html`; replace `VIDEO_ID` in its iframe URL with the ID from your YouTube video.
